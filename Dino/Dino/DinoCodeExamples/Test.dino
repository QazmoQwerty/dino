// if C == 0:
//     H = null
// else if V == r:
//     H = (g - b) / C
// else if V == g:
//     H = (b - r) / C + 2
// else:
//     H = (r - g) / C + 4


switch {
    case a == 1 {
        x += 10
        return x
    }
    case b == 2:
        return 10
    default:
        return x + 10
    }
}

switch A.Area() {
    case a {
        x += 10
        return x
    }
    case b:
        return 10
    default:
        return x + 10
    }
}