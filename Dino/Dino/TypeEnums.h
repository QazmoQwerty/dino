#pragma once

enum TokenType
{
	NEWLINE,
	OPERATOR,
	LINE_BREAK,
	IDENTIFIER,
	LITERAL,
};

enum LiteralType
{
	NULL,
	BOOLEAN,
	INTEGER,
	FRACTION,
	CHARACTER,
	STRING,
};

enum OperatorType
{
	// TODO
};