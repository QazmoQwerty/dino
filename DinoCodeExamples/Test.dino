int Mul(int a, int b) {
    return a * b
}

int Pow(int a, int n) {
    int ret := 1
    for int i := 0 | i < n | i += 1:
        ret := Mul(ret, a)
    return ret
}

int Main() {
    return Pow(2, 10)
}