//include "DinoCodeExamples/LinkedList.dino"

namespace Prog {
	// int putchar(int a): extern
	// char getchar(): extern
	// char Print(char c): return putchar(c as int) as char
	// void Newline(): Print('\n')
 
	// void PrintNum(int num) {
    //     int a ≡ 1
    //     while a ≤ num / 10: a *= 10
    //     while a > 0 {
    //         putchar(num % (a * 10) / a + 48)
    //         a /= 10
    //     }
    // }

	// void PrintArr(int[20] arr): 
	// 	for int i := 0 | i < arr.Size | i+=1 { PrintNum(arr[i]) | Newline() }

	// //void Foo((char(char))@ f): (@f)('a')
	// const c1 ≡ 'a'
	// const c2 ≡ '\n'

	// namespace NS: const c1 ≡ '\n'

	int Main() {
		//var a := [1, 2, 3]
		// int[20] a
		// // char a ≡ '0'
		// // char b ≡ '1'
		// // a, b ≡ b, a

		// for int i := 0 | i < a.Size | i+=1 {
		// 	a[i] := (i + 1) * (i + 1)
		// }

		// PrintArr(a)

		// // for int i := 0 | i < 10 | i+=1 {
		// // 	PrintNum(a[i]) | Newline()
		// // }
		
		// PrintNum(a.Size) | Newline()
		char a := 20 as char
		char b := 30 as char
		char d := a + b
		// char c := getchar()
		// Newline() | Print(c) | Newline()

		// //putchar('0' as int + a[1])
		//Newline()
		return d as int
		// Foo(&Print)
		// Print('h')
		// Print('w')
		// return Print('\n') as int
	}
}